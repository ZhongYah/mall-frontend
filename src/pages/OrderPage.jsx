import { Container, Typography } from '@mui/material';

export default function OrderPage() {
  return (
    <Container>
      <Typography variant="h4">歷史訂單頁面</Typography>
      <Typography>這裡會顯示歷史訂單（暫時空白）</Typography>
    </Container>
  );
}
